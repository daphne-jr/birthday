import { atom } from 'jotai';

export const scoreAtom = atom(0);
export const levelAtom = atom(0);
export const wordAtom = atom('');
export const inputAtom = atom('');
export const correctAtom = atom([]);
